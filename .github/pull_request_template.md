## Change

Describe the behavior change and link the related issue, if any.

## Verification

- [ ] `dotnet build SystemOneSharp.slnx -c Release`
- [ ] `dotnet run --project tests/SystemOneSharp.Verification -c Release`

List any additional checks and their results. Remove secrets and private response data from logs.
